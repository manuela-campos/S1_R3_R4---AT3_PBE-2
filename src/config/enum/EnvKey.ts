export enum EnvKey{

    // porta servidor
    SERVER_PORT = 'SERVER_PORT',

    // variaveis de conexão com o db
    DB_HOST = 'DB_HOST',
    DB_PORT = 'DB_PORT',
    DB_DATABASE = 'DB_DATABASE',
    DB_USER = 'DB_USER',
    DB_PASSWORD = 'DB_PASSWORD'
}